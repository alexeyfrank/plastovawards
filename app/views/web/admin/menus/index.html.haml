%h1 Список меню

%br
= link_to "Добавить меню", new_admin_menu_path, class: 'btn btn-primary'
%br
%br

%table.table.table-bordered
  %thead
    %tr
      %th.id-col #
      %th Название
      %th.count-col Количество элементов
      %th.actions-col
  %tbody
    - @menus.each do |menu|
      %tr
        %td.id-col= menu.id
        %td= link_to menu.name, edit_admin_menu_path(menu)
        %td.count-col= menu.menu_items.count
        %td.actions-col
          = link_to "Редактировать", edit_admin_menu_path(menu), class: 'btn btn-info'
          = link_to "Элементы меню", admin_menu_menu_items_path(menu), class: 'btn btn-success'
          = link_to "Удалить", admin_menu_path(menu), method: :delete, class: 'btn btn-alert'
